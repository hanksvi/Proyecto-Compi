proc calcularConstante(): int =
    var temp: int
    temp = 100 + 50 * 2
    
    if true:
        temp = temp + 50
    else:
        temp = 0
    
    if false:
        return 0
    else:
        return temp

proc evaluarCondiciones(): bool =
    if 10 > 5:
        if 20 >= 15:
            return true
        else:
            return false
    else:
        return false

var x: int
var flag: bool
var y: float
x = calcularConstante()
echo(x)

flag = evaluarCondiciones()
echo(flag)


y = 2.0 * 3.0 + 4.0 * 5.0
echo(y)

if flag:
    y = y + 10.0
    echo(y)
