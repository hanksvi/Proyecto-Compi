var a, b, c: int

proc cuadrado(x:int): int =
    return(x * x)

proc suma(a:int, b:int): int =
    return(a + b)

proc operacion(x:int, y:int): int =
    var m: int
    m = cuadrado(x)
    m = m + suma(x, y)
    return(m)

a = 3
b = 4
c = operacion(a, b)
echo(c)
